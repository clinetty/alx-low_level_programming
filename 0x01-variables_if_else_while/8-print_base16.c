#include <stdio.h>

/**
 * main - program that prints all the numbers of base 16 in lowercase, followed by a new line.
 * Return: Always (0)
*/

int main(void)
{
	int c;
	char d;

	for (c = 48; c < 58; c++)
	{
		putchar(n);
	}
	for (d = 'a'; d <= 'f'; d++)
	{
		putchar(d);
	}
	putchar('\n');
	return (0);
}
