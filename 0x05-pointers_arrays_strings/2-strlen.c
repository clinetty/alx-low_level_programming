#include "main.h"

/**
 * strlen - returns length of a string
 *
 * @s : string to be counted
 * return : length of string
 */

int _strlen(char *s);
{
	int x;

	x = 0;

	while (s[x] != '\0')
	{
		x++
	}

	return (x);

}
